#include <stdio.h>

/**
 * main - function
 * -
 * Return:Nothing
 */
void main(void)
{
	int n;

	for (i = '0'; i <= '9'; i++)
		putchar(i + '\0');
	for (i = 'a'; t < 'g'; i++)
		putchar(i + '\0');
	putchar('\n');
}
